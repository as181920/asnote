<b><%= link_to @note_name, note_records_path(@note_id) %>: 添加列... </b>
<br/>
<br/>
<%= form_tag note_labels_path(@note_id) do %>
  <%= render "label_form" %>
  <br/>

  <%= submit_tag("添加") %>
<% end %>
