<b><%= link_to @note_name, note_records_path(@note_id) %>: editing...</b>
<br/>
<br/>
<%= form_tag note_label_path(@note_id, @id), method: "put" do %>
  <%= render "label_form" %>
  <br/>

  <%= submit_tag("更新") %>
<% end %>
