<b><%= link_to @note_name, note_records_path(@note_id) %>: add...</b>
<br/>
<br/>
<%= form_tag note_records_path(@note_id) do %>
  <%= render "record_form" %>

  <%= submit_tag("新增") %>
<% end %>

